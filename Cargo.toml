[package]
name = "rust-sed"
version = "0.1.0"
edition = "2024"
description = "A GNU sed-compatible stream editor written in Rust"
license = "MIT"

[[bin]]
name = "sed"
path = "src/main.rs"

[dependencies]
fancy-regex = "0.14"
regex = "1"

[profile.release]
strip = true
lto = true
